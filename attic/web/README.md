## Deprecated: `attic/web`

This directory is **not used** by PageForge.

- The canonical UI lives in `frontend/` and is the only frontend wired into the repo tooling (`Makefile`, root docs).
- `attic/web` is a quarantined Vite template/prototype kept only for reference.

If you’re looking to run the app:

```bash
make dev-frontend
```

If you need the original template README, see `VITE_TEMPLATE_README.md`.

